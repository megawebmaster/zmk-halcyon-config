# Ownership for files the Docker firmware build writes back to ./firmware.
# docker-compose.yml reads $UID/$GID; bash sets UID itself (and marks it readonly), so
# it only needs exporting, while GID has to be derived.
export UID
export GID="$(id -g)"
